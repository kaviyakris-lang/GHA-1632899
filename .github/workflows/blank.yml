name: demo
on: 
  push:
    branches:
      - main
  pull_request: 
    branches:
      - main     
jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: print name
        run: |
          echo "my name is ${{ vars.NAME }}"
          echo "i am working in ntt data"
          
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: deploy 
        run: echo "${{ github.repository }}"
